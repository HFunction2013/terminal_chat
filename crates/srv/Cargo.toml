[package]
name = "srv"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
prost = "0.13"
protos = { path = "../protos" }
# prost-types = "0.14"
tonic = "0.12"
tokio = { version = "1", features = ["full"] }
[package]
name = "translations"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["dylib"]

[dependencies]
deeplx = { version = "2", default-features = false }
tokio = { version = "1", features = ["full"] }
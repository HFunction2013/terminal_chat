syntax = "proto3";

package models.system;

// 系统信息
message SystemInfo {
    string name = 1; // 系统名称，如 "Windows", "Linux", "macOS"
    string arch = 2; // 架构，如 "x86_64", "arm64", "i386"
    string bits = 3; // 位数，如 "32", "64"
    string vers = 4; // 系统版本号，如 "10.0.19045" 或 "22.04"
}